<div class="container">
	<h1 class="text-center">Sup Yall it's me!</h1>
	<div class="row">
		<div class="col-6 text-center">
			<ul>
				{{#each burgers}}
					{{#unless this.devoured}}
						<li>{{this.burger_name}}</li>
					{{/unless}}
				{{/each}}
			</ul>
		</div>
		<div class="col-6 text-center">
			<ul>
				{{#each burgers}}
					{{#if this.devoured}}
						<li>{{this.burger_name}}</li>
					{{/if}}
				{{/each}}
			</ul>
		</div>
	</div>	
</div>